# ✅ 체크리스트: Bing IndexNow 연동 이슈 해결 및 검증

## 📅 날짜: 2026-04-19
## 🎯 목표: Bing IndexNow 기능이 실제 사이트(news.lego-sia.com)에서 정상 작동하도록 복구하고 검색 노출 가속화.

---

### 1️⃣ 단계: 환경 및 설정값 정합성 점검
- [ ] `static/bbd0d9a6843c450eb3e9d811a0fd504a.txt` 파일 존재 및 내용 일치 확인.
- [ ] `robots.txt` 내 `Disallow` 항목 수정 (이전 키값 제거 및 현재 키값 `Allow` 추가).
- [ ] `hugo.toml` 내 다국어 설정(Language Selection)이 루트 파일 접근을 방해하는지 확인.

### 2️⃣ 단계: Hugo 빌드 결과물 검증
- [ ] `npx hugo` 빌드 후 `public/` 루트에 키 파일이 위치하는지 확인.
- [ ] `public/robots.txt`에 수정 사항이 반영되었는지 확인.
- [ ] 로컬 서버(`hugo server`) 환경에서 `localhost:1313/bbd0d9a6843c450eb3e9d811a0fd504a.txt` 접근 가능 여부 테스트.

### 3️⃣ 단계: 자동화 파이프라인 연동 점검
- [x] `automation/indexnow_service.py` 내 호스트(`news.lego-sia.com`) 및 키값 최신화 확인.
- [x] IndexNow 페이로드 최적화 (keyLocation 제거 및 User-Agent 헤더 추가).
- [ ] `news_main.py` 실행 시 기사 생성 후 `notify_indexnow` 함수가 실제 호출되는지 로그 확인.
- [ ] GitHub Actions 환경에서 해당 API 호출이 차단되지 않는지 확인.

### 4️⃣ 단계: 외부 검증 및 모니터링
- [x] Bing Webmaster Tools 키 정합성 확인 (bbd0d9a6843c450eb3e9d811a0fd504a).
- [x] 수동 최적화 버전 API 호출 테스트 (Status 200/202 확인).
- [ ] Bing 검색 엔진 반영 대기 (수 시간~1일 소요 예상).


---

### 🚀 리스크 및 특이사항
- **i18n 이탈**: Hugo 설정에 따라 모든 정적 파일이 `/ko/` 폴더 안으로 들어갈 경우 Bing이 루트에서 키를 찾지 못할 수 있음.
- **robots.txt 충돌**: 현재 `robots.txt`에 잘못된 키값이 `Disallow` 되어 있어 Bing이 접근을 거부할 수 있음.
