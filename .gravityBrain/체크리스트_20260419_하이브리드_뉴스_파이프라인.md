# ✅ 체크리스트: 하이브리드 뉴스 파이프라인 (Legacy 복구 및 고도화)

## 📅 날짜: 2026-04-19
## 🎯 목표: 기존 API 기반 수집 로직을 복구하고, NotebookLM 버전과 동일한 수준의 고퀄리티 문서를 생성하도록 업그레이드.

---

### 1️⃣ 단계: 인프라 및 분류 체계 정비
- [x] `ai_news_editor.py` 내 카테고리 설정을 Premium 클러스터(`ai`, `hardware`, `insights`)로 통합 매핑.
- [x] `news_main.py`의 `CAT_MAP` 및 `FALLBACK_MAP`을 최신 클러스터 기준에 맞춰 정규화.
- [x] `image_manager.py`가 모든 파이프라인(Legacy/Premium)에서 공통으로 작동하는지 확인.

### 2️⃣ 단계: Gemini 기반 에디팅 로직 고도화
- [x] `ai_news_editor.py`의 프롬프트를 NLM(Mode B) 수준으로 강화.
    - [x] 단순 요약 지양, "Why & So What" 분석 포함.
    - [x] 비판적 시각(Critical Perspective) 및 기술적 깊이(Professional Depth) 강제.
    - [x] "AI Filler" 문구(예: ~할 것으로 기대된다) 배제 로직 강화.
- [x] `AIWriter`에서 `gemini-3.1-flash-lite` (또는 최신버전) 및 `google/gemma-3-27b-it:free`를 최우선 모델로 설정.
- [x] 모든 API 요청 전 대기 시간(Throttling)을 20초로 상향 조정.

### 3️⃣ 단계: 파이프라인 통합 및 하이브리드 분기
- [x] `news_main.py`에서 `--mode legacy` 실행 시에도 `image_manager`의 Tiered Strategy를 완벽히 타도록 수정.
- [x] 기사 슬러그 생성 규칙을 Premium 방식(`{cluster}-{slug}`)으로 통일하여 중복 방지 및 SEO 최적화.
- [x] `StateTracker`를 통해 Legacy와 Premium 기사가 서로 겹치지 않게 관리.

### 4️⃣ 단계: 검증 및 테스트
- [x] `python automation/news_main.py --mode legacy --limit 1` 테스트 실행.
- [x] 생성된 마크다운 문서의 퀄리티(내용의 깊이, 포맷팅, 이미지 경로) 검토.
- [x] 한/영 동시 발행 및 이미지 공유 로직 정상 작동 확인.

### 5️⃣ 단계: 콘텐츠 미러링 및 고도화 (Ironclad Protocol v1.1)
- [x] **2-Pass 아키텍처**: 영문 분석 리포트 생성(Step 1) 후 국문 미러링 현지화(Step 2)로 품질 극대화.
- [x] **표준 통합 스키마**: Legacy와 Premium(NLM)의 데이터 필드 및 한/영 양식 완전 일원화.
- [x] **템플릿 엔진 리팩토링**: `news_main.py`에서 언어별 분기 제거 및 표준 필드 기반 자동 렌더링.
- [x] **모델명 픽스**: `gemini-3.1-flash-lite-preview` 실검증 및 적용 완료.

---

- 모든 API 요청 후 **10초 강제 대기**를 통해 안정적인 무료 티어 할당량 유지 및 속도 개선.
- 국문 헤더 **'상세 분석'**, **'시사점'**으로 통일 및 본문 중복 이미지 제거 완료.
- **5단계 폴백 시스템**: Gemini 3.1 실패 시 **Gemini 3 Flash (Paid)** 및 OpenRouter/Groq 대안 엔진 즉시 투입 구조 확립.
- **GitHub Actions 최적화**: `git pull --rebase` 및 원자적 발행 로직 적용으로 배포 안정성 확보.
