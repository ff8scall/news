# 🧠 MEMORY: 뉴스 인텔리전스 시스템

## 📍 현재 상태
- **날짜**: 2026-04-19
- **단계**: 프리미엄 뉴스 파이프라인 고도화 완료 (이미지 최적화 포함)
- **최근 결정**: 
    - NLM 리포트를 개별 기사로 분리하는 `Mode B`를 기본으로 설정.
    - 대분류(Cluster)를 `ai`, `hardware`, `insights`로 단순화 및 정규화.
    - 한글 제목만 있는 경우에도 영문 슬러그를 생성하는 방어 로직(`premium-{cat}-{id}`) 구축.
    - 불필요한 과거 콘텐츠 및 가이드/분류 데이터 정리 완료 (2026-04-19 데이터만 유지).
    - **이미지 최적화 완료**: `image_manager.py` 도입으로 계층적 이미지 전략 수립 및 자동 캐싱 라이브러리 구축.

## 🎯 단기 목표
- [x] 이미지 최적화 Tiered Strategy 구현 및 통합
- [x] 키워드 기반 이미지 라이브러리 자동 구축 로직 완성
- [ ] RSS 소스 추가 및 필터링 정교화 (다음 단계)
