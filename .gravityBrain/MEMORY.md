# 🧠 MEMORY: 뉴스 인텔리전스 시스템

## 📍 현재 상태
- **날짜**: 2026-04-22 (v6.5 무한 루프 해결 및 크론 최적화)
- **해결**: NLM 리포트 실패 시 무한 폴링하는 버그 수정 (`failed` 시 즉시 중단).
- **성공**: 21시 배지 미발행분(24건) 수동 복구 및 발행 전수 완료.
- **조정**: 크론탭 스케줄 정상화 (`0 5,11,17,21` - 23시 배지 제거).
- **상태**: 파이프라인 전 구간 안정화.
- **최근 결정**: 
    - **[v6.4] 배치 최적화 및 데이터 무결성**: 17시 배치에서 발생한 데이터 유실 문제를 해결하기 위해 Job 리스트 저장 로직을 Append 방식으로 전환하고, Threshold=5 정책을 적용하여 기사 발행 품질을 극대화했습니다.
    - **[v6.1] 실시간 영문 복구 및 AI 안정화**: NLM의 영문 본문 누락 시 Gemini 2.0 Flash를 통한 실시간 번역 복구 로직을 구현했습니다. 모델 이름 교정 및 다중 폴백 시스템을 통해 11시 배치 24건을 성공적으로 복구 완료했습니다.
    - **NLM Batch Optimization**: NLM 분석 품질 보장을 위해 세션당 카테고리별 기사 수를 10개로 제한.
    - **Deferral Logic (이월 로직)**: HarvesterV3를 수정하여, 품질은 높으나 이번 배치에서 제외된 기사를 `seen_urls`에 기록하지 않음으로써 다음 스케줄(6시간 후)에 자동으로 재수집되도록 구현. [v5.3]
    - **Bing SEO (보완)**: 메타 태그의 인용구 생략 문제 해결을 위해 `safeHTML`을 적용하고, 교차 검증을 위한 `static/BingSiteAuth.xml` 파일을 추가 완료. [v5.2]
    - **Bing SEO**: 사이트 소유권 인증을 위한 메타 태그(`msvalidate.01`)를 `hugo.toml` 및 `layouts/partials/seo.html`에 반영 완료. [v5.1]
    - **Google Indexing**: 실시간 색인을 위해 Google Indexing API를 `automation/google_indexing_service.py`로 구현 및 `indexnow_service.py`에 통합.
    - **Security**: 서비스 계정 JSON 키를 `automation/keys/` 폴더로 이동하고 `.gitignore`에 추가하여 보안 강화.
    - **[v5.1] 지능형 필드 추론(Intelligent Inference)**: NLM이 `KOR_CONTENT:` 마커를 누락하고 바로 `##` 헤더를 쓸 경우, 언어(한글)와 헤더 패턴을 분석하여 자동으로 본문을 할당하는 지능형 폴백을 적용했습니다.
    - **[v5.2] 영문 기사 복구 및 엄격한 언어 매핑**: 05시 배치 시 발생한 영문 기사 누락(한글 제목 노출) 문제를 해결하기 위해, 영문 필드 누락 시 국문을 그대로 복사하지 않고 `Recovery:` 접두사를 붙여 격리하도록 파서 로직을 강화했습니다. 또한, 이미 발행된 31건의 기사를 국문 데이터를 기반으로 영문으로 완벽 복구 완료했습니다. [v5.2]
    - **[v5.3] 크론탭 환경 변수 주입 및 23시 배치 긴급 복구**: 크론탭 환경에서 `.env` 파일을 자동으로 로드하도록 `nlm_orchestrator.py`를 개선하여 `GEMINI_API_KEY` 인식 문제를 해결했습니다. 또한 21일 23시 배치의 영문 누락 15건을 추가로 복구 완료했습니다. [v5.3]
    - **[v5.4] 스마트 이월(Backlog) 시스템 도입 및 한도 축소**: NLM 출력 안정성을 위해 카테고리당 발행 한도를 10개에서 8개로 하향 조정했습니다. 한도를 초과한 고품질 기사는 `backlog.json`에 영구 보관되어 다음 배치 시 최우선적으로 처리됩니다. (48시간 유효) [v5.4]
    - **Git Sync**: 원격 저장소의 최신 변경 사항(이미지 다각화 v6.1, 프리미엄 뉴스 업데이트 등 14개 커밋)을 로컬로 Pull 및 병합 완료하여 동기화 상태를 유지함.

## 🎯 단기 목표
- [x] 이미지 최적화 Tiered Strategy 구현 및 통합
- [x] 하이브리드 뉴스 파이프라인 (Legacy + Premium) 고도화 및 통합 완료
- [x] 국문 기사 헤더 통일 및 중복 제거 (상세 분석, 시사점)
- [x] API 기반 기사 3건 생성 테스트 (완료)
- [x] IndexNow Naver 통합 완료 (News 전용 키 생성 및 자동화 로직 고도화) [v1.8]
- [x] Ubuntu 클라우드 이전 및 NLM 인증 자동화 타당성 검토 완료 (Stay-alive 전략 수립) [v1.9]
- [x] 실운영 자동화 스케줄링 검증 (05, 11, 17, 21시 맞춤 설정 완료) [v2.0]
- [x] NLM 클라우드 배치 최적화 (단계별 알림 및 에러 리포트) 완료 [v2.1]
- [x] 파이프라인 중단 시(0건) 텔레그램 알림 보완 완료 [v2.2]
- [x] RSS 소스 추가 및 글로벌 시차 보완 (영국, 일본, 대만 매체 통합) 완료 [v2.3]
- [x] Gemini 1.5 Flash 기반 '라이트 스코어러' 필터링 도입 및 로직 정상화 [v2.4]
- [x] NLM 파서 고도화 및 이미지 폴백 경로(엑박) 긴급 수정 완료 [v4.7]
- [x] 국문 제목 누락 복구 로직 및 요약 트렁케이션 완화 자동화 완료 [v4.9]
- [x] Bing 사이트 소유권 인증 메타 태그 추가 완료 [v5.1]
- [x] 수작업으로 발행된 영문 기사 31건 복구 및 제목 정상화 [v5.2]
- [x] NLM 파서 엄격 언어 매핑 로직(v5.2) 적용 및 배포
- [x] 깃 원격지 최신 변경 사항 동기화 및 병합 완료
- [x] 21일 23시 배치 영문 누락 기사 15건 긴급 복구 완료 [v5.3]
- [x] 크론탭 환경 변수 로드 로직 최적화 및 안정화 완료 [v5.3]
- [x] 스마트 이월(Backlog) 시스템 구축 및 한도(8개) 축소 완료 [v5.4]
- [ ] 백로그 시스템 도입 후 기사 누적 속도 및 품질 모니터링 (Next)
- [ ] 메가트렌드 분석 및 성능 비교 콘텐츠 보완
